Apply the review fixes for the scroll-performance and reCAPTCHA changes (analysis delivered in the review above).

1. **`content/site-settings.json`** — set `lenis.lerp` back to `0.1` (documented 0–1 range). Verified in installed Lenis 1.3.25 source that `lerp` is ignored whenever `duration` + easing are set, so 1.2 was dead config; `duration: 0.8` stays as the effective tuning.

2. **`src/components/site/SiteHeader.tsx`** — extract a shared `syncHeaderHeight(header, compact)` helper used by both effects; introduce a named constant for the post-transition sync delay (transition 300ms + buffer = 320ms) documented against the CSS transition; add a one-shot `document.fonts.ready.then(...)` re-sync in the mount effect to cover font-swap height changes previously handled by the removed ResizeObserver.

3. **`src/app/api/jazzcash/initiate/route.ts` and `src/app/api/jazzcash/portal/initiate/route.ts`** — reorder handlers: run cheap field validation (donorName, mobile, amount, etc.) BEFORE `verifyRecaptchaToken`, so validation errors don't consume the user's single-use captcha token. Keep rate-limit-first ordering.

4. **`src/lib/recaptcha.ts`** — add `signal: AbortSignal.timeout(10_000)` to the siteverify fetch and map the abort to the existing failure path.

5. **`src/components/forms/Recaptcha.tsx`** — cap the existing-script polling interval (e.g. 15s); on timeout, clear the interval and call `onError` instead of polling forever.

6. **`src/components/forms/DynamicForm.tsx`** — add a brief code comment noting the captcha token is client-side only until a form-submission endpoint exists (no behavior change).

Verification: run lint/typecheck on touched files (`npm run lint` / `npx tsc --noEmit` or project equivalent) after edits.